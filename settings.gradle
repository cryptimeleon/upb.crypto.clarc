rootProject.name = 'clarc'

if (file("../upb.crypto.math/.composite-enable").exists()) {
    includeBuild ('../upb.crypto.math')
}
if (file("../upb.crypto.protocols/.composite-enable").exists()) {
    includeBuild ('../upb.crypto.protocols')
}

